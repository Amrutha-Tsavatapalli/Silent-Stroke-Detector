# Training Hooks

This folder is reserved for future work:

- Landmark feature extraction datasets
- Voice feature datasets
- Fusion model training notebooks or scripts
- Exported `joblib` models for production scoring

The runtime currently uses rule-based fusion unless a trained model is added at `artifacts/fusion_model.joblib`.
